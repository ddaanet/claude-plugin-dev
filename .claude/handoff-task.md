## Current task

Designing how a plugin's first release chooses its version (brief `plans/2026-09-07-brief-first-release-version-selection.md`, triaged Moderate via /edify:design). My human partner settled the rules, recorded in `plans/2026-09-15-first-release-version/outline.md`: the initial manifest version (seeded 0.1.0 by `/plugin-dev:create-plugin`) is the initial release; initial release is detected solely by the absence of a `^v[0-9]+\.[0-9]+\.[0-9]+$` tag; `just release` then refuses any argument and publishes the manifest version. The opus outline-corrector has reviewed and fixed the outline (report in `reports/outline-review.md`). A /proof pass was started and stopped before item 1 so a Codex review could run first.
